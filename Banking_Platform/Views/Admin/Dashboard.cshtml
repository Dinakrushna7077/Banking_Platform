
@{
    ViewBag.Title = "Dashboard";
}

<link href="~/Content/Site.css" rel="stylesheet" />
<script src="~/Scripts/jquery-3.7.1.min.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
<script src="~/Scripts/bootstrap.min.js"></script>
<style>
    .toggle-btn {
        cursor: pointer;
    }

    .dropdown-item {
        color: #333;
        height: 3rem;
        display: flex;
        align-items: center;
    }

        .dropdown-item:hover {
            background-color: white;
            color: #007bff;
        }
</style>
<div class="container-xxl position-relative bg-white d-flex p-0">
    <div class="sidebar pe-4 pb-3 position-sticky top-50 col-3">
        <nav class="navbar ">
            <div class="navbar-nav w-100">
                <a href="Dashboard" class="nav-item nav-link active"><i class="fa fa-tachometer-alt me-2"></i>Dashboard</a>
                <div class="nav-item dropdown">
                    <div class="nav-item dropdown">
                        <a class="nav-link toggle-btn" id="drdn-toggle-1">
                            <i class="fa fa-laptop me-2"></i>Transaction
                        </a>
                        <div class="drdn-item-1 bg-transparent border-0 d-none">
                            @Ajax.ActionLink("Deposit", "Deposit", "Admin", new AjaxOptions() { HttpMethod = "Get", InsertionMode = InsertionMode.Replace, UpdateTargetId = "tgt" }, new { @class = "dropdown-item" })
                            @Ajax.ActionLink("Withdraw", "Withdraw", "Admin", new AjaxOptions() { HttpMethod = "Get", InsertionMode = InsertionMode.Replace, UpdateTargetId = "tgt" }, new { @class = "dropdown-item" })
                            @Ajax.ActionLink("Transfer", "Transfer", "Admin", new AjaxOptions() { HttpMethod = "Get", InsertionMode = InsertionMode.Replace, UpdateTargetId = "tgt" }, new { @class = "dropdown-item" })
                        </div>
                    </div>
                </div>
                <div class="nav-item dropdown">
                    <a class="nav-link toggle-btn" id="drdn-toggle-2"><i class="far fa-file-alt me-2"></i>Account</a>
                    <div class="drdn-item-2 bg-transparent border-0 d-none">
                        @Ajax.ActionLink("New Account", "New_Account", "Admin", new AjaxOptions() { HttpMethod = "Get", InsertionMode = InsertionMode.Replace, UpdateTargetId = "tgt" }, new { @class = "dropdown-item" })
                        @Ajax.ActionLink("Modify", "Modify", "Admin", new AjaxOptions() { HttpMethod = "Get", InsertionMode = InsertionMode.Replace, UpdateTargetId = "tgt" }, new { @class = "dropdown-item" })
                        @Ajax.ActionLink("Balance Inquary", "Balance_Inquary", "Admin", new AjaxOptions() { HttpMethod = "Get", InsertionMode = InsertionMode.Replace, UpdateTargetId = "tgt" }, new { @class = "dropdown-item" })
                    </div>
                </div>
            </div>
        </nav>
    </div>
    <div" class="w-100">
        <div id="tgt" class="w-100">
            <div class="container-fluid pt-4 px-4">
                <h3 class="text-center">
                    <span class="text-success">You’ve got this. </span> <span class="text-primary">Let’s do great work today!</span>
                </h3>
            </div>
            <div class="container-fluid pt-4 px-4">
                <div class="row g-4">
                    <div class="col-sm-12 col-md-6 col-xl-6">
                        <div class="h-100 bg-light rounded p-4">
                            <div class="d-flex align-items-center justify-content-between mb-2">
                                <h6 class="mb-0">Messages</h6>
                                <a href="">Show All</a>
                            </div>
                            <div class="d-flex align-items-center border-bottom py-3">
                                @*<img class="rounded-circle flex-shrink-0" src="img/user.jpg" alt="" style="width: 40px; height: 40px;">*@
                                <div class="w-100 ms-3">
                                    <div class="d-flex w-100 justify-content-between">
                                        <h6 class="mb-0">Jhon Doe</h6>
                                        <small>15 minutes ago</small>
                                    </div>
                                    <span>Short message goes here...</span>
                                </div>
                            </div>

                        </div>
                    </div>


                </div>
            </div>
        </div>
</div>

<script>
    $(document).ready(function () {

        function toggleDropdown(triggerSelector, contentSelector) {
            var $content = $(contentSelector);
            var $trigger = $(triggerSelector);

            // Toggle content
            if ($content.hasClass('d-none')) {
                $content.removeClass('d-none').hide().slideDown('slow');
                $trigger.addClass('active');
            } else {
                $content.slideUp('slow', function () {
                    $content.addClass('d-none');
                });
                $trigger.removeClass('active');
            }
        }

        $('#drdn-toggle-1').on('click', function () {
            // Remove active from the other toggle
            /*$('#drdn-toggle-2').removeClass('active');*/
            toggleDropdown('#drdn-toggle-1', '.drdn-item-1');
        });

        $('#drdn-toggle-2').on('click', function () {
            /*$('#drdn-toggle-1').removeClass('active');*/
            toggleDropdown('#drdn-toggle-2', '.drdn-item-2');
        });

    });
</script>



@*@Html.Partial("_Deposit")*@